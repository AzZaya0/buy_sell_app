import 'package:flutter/material.dart';

class BlockDealTab extends StatelessWidget {
  const BlockDealTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('hello'),
    );
  }
}
